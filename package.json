{
  "name": "pi-ip",
  "version": "1.0.0",
  "description": "Publishes the ip address of the host computer to a webserver on a remote machine, where it is displayed on a page with a well-known url. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Grogan",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^11.0.0",
    "localtunnel": "^1.8.2"
  }
}
